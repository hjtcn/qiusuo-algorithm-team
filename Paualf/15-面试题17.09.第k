/*
有些数的素因子只有 3，5，7，请设计一个算法找出第 k 个数。注意，不是必须有这些素因子，而是必须不包含其他的素因子。例如，前几个数按顺序应该是 1，3，5，7，9，15，21。
示例 1:
输入: k = 5
输出: 9
*/

Clarification：
如果一个数的素因子只有 [3,5,7] 的话是符合题意的，那么怎么判断一个数的素因子是什么呢？
没有想出来，就去看了题解，题解大部分写的都是，三指针 + 动态规划
题目还提示 明确这个问题的要求。要求满足 3a× 5b× 7c 这一形式的第 k 小的值。

func getKthMagicNumber(k int) int {
    p3,p5,p7 := 0,0,0
    dp := make([]int, k)
    dp[0] = 1
    for i := 1;i < k;i++ {
        dp[i] = min(dp[p3]*3, min(dp[p5] * 5,dp[p7] * 7));
        if dp[i] == dp[p3] * 3 {
            p3++
        }
        if dp[i] == dp[p5] * 5 {
            p5++
        }
        if dp[i] == dp[p7] * 7 {
            p7++
        }
    }
    return dp[k - 1]
}
func min(a,b int)int {
    if a < b {
        return a
    }
    return b
}

复杂度分析：
时间复杂度：O(n) 遍历 从 1 -> k
空间复杂度：O(n): 开辟dp数组存储数据

我感觉这道题更像是数学题，考察的是数学公式，可能是数学功底没那么强，我还是觉得理解的不是很清晰，动态规划找动态方程的时候，也是用数学思维去分析和解决

总结：
1. 没有明白题意，对题目理解的不是很到位
2. 感觉更像是考察数学，不是在考察程序和代码，数学也是解决问题的一种方式，数学知识基础不足
